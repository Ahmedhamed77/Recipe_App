import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  searchBar: {
    display: 'flex',
    flexGrow: 1,
    alignItems: 'center',
    paddingRight: 5,
    flexDirection: 'row',
    backgroundColor: '#eeeeee',
    borderColor: '#f6f6f6',
    borderWidth: 1,
    borderRadius: 10,
  },
  searchInput: {
    paddingHorizontal: 8,
    height: 38,
  },
});
