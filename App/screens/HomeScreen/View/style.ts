import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
  },
  screenContainer: {
    paddingHorizontal: 8,
    backgroundColor: '#FFFF',
  },
  headerContainer: {
    marginBottom: 8,
  },
  headerText: {
    color: '#000000',
    fontSize: 34,
    fontWeight: 'bold',
    paddingBottom: 12,
  },
});
